import { BestDirectiveDirective } from './best-directive.directive';

describe('BestDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new BestDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
